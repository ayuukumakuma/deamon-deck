# Phase 6: ファイル監視 + 仕上げ

## 概要
ファイルシステム監視による自動更新、ダークモード対応、キーボードショートカット等の仕上げを行う。

## 前提
- Phase 5 完了（主要機能が全て動作する状態）

## タスク

### Rust: ファイルシステム監視
- [x] `src-tauri/src/watcher.rs` を作成
- [x] `notify` クレートで `~/Library/LaunchAgents/` を監視
  - [x] ファイル作成 (Create) イベント
  - [x] ファイル変更 (Modify) イベント
  - [x] ファイル削除 (Remove) イベント
- [x] イベント発生時に Tauri グローバルイベント `services-changed` を送信
- [x] デバウンス処理（短時間に複数イベントが発生した場合にまとめる）
- [x] Tauri `setup` フックで監視を開始
- [x] アプリ終了時にウォッチャーをクリーンアップ

### ダークモード
- [x] `src/hooks/useTheme.ts` を作成
  - [x] `prefers-color-scheme` メディアクエリの監視
  - [x] テーマ状態: `light` / `dark` / `system`
- [x] CSS 変数によるテーマ切替
  - [x] ライトモードのカラーパレット
  - [x] ダークモードのカラーパレット
  - [x] `@media (prefers-color-scheme: dark)` でシステム連動
- [x] 全コンポーネントのダークモード対応確認
  - [x] サイドバー
  - [x] サービステーブル
  - [x] 詳細パネル
  - [x] Plist エディタ（CodeMirror テーマ含む）
  - [x] ログビューア

### Tauri ウィンドウ設定
- [x] macOS vibrancy 設定
  - [ ] サイドバーに `vibrancy: "sidebar"` 効果
  - [x] Tauri ウィンドウの `transparent` 設定
- [x] タイトルバーのカスタマイズ
  - [x] トラフィックライト（信号ボタン）の位置調整
  - [x] ドラッグ可能領域の設定

### キーボードショートカット
- [x] `Cmd+R` - サービス一覧をリフレッシュ
- [x] `Cmd+S` - Plist エディタで保存
- [x] `Cmd+Shift+S` - 保存 & リロード
- [x] `Cmd+N` - 新規サービス作成
- [x] `Cmd+F` - 検索フィールドにフォーカス
- [x] `Escape` - 検索クリア / ダイアログ閉じ
- [x] `↑` / `↓` - サービス一覧のナビゲーション

### エラーハンドリング改善
- [x] グローバルエラーバウンダリ
- [x] ネットワーク/ファイルシステムエラーのリトライ UI
- [x] launchctl エラーのユーザーフレンドリーなメッセージ変換

### 空状態 / ローディング状態
- [x] サービス一覧が空の場合のプレースホルダー
- [x] 初回読み込み中のスケルトン/スピナー
- [x] サービスが選択されていない場合のウェルカムメッセージ

### パフォーマンス
- [ ] サービス一覧の仮想スクロール（サービス数が多い場合）
- [ ] plist 読み込みのキャッシュ（変更時のみ再読み込み）
- [x] 不要な再レンダリングの防止

### アクセシビリティ
- [x] キーボードナビゲーション対応
- [x] ARIA ラベルの設定
- [x] フォーカス管理

## 完了条件
- `~/Library/LaunchAgents/` でファイルが追加/変更/削除されると自動的にサービス一覧が更新される
- ダークモード/ライトモードがシステム設定に連動する
- 主要操作がキーボードショートカットで実行できる
- エラー時に分かりやすいメッセージが表示される
- 空状態やローディング状態が適切に表示される
